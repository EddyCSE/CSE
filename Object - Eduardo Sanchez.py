class Balloon(object):
    def __init__(self):
        self.hole = False
        self.obtained = True
        self.water_filled = False
        self.air_filled = False
        self.shiny = True

    def fill_with_water(self):
        if self.air_filled and self.water_filled = False
            self.water_filled = True
        else:
            print("You can't")

    def fill_with_air(self):
        if self.water_filled = False:
            self.air_filled = True

    def let_go(self):
        if self.hole = True:
            print("You can't, it's broken")
            return
        if self.

    def throw(self):
        if

    def squeak(self):
        if